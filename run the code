▶️ Run the Website

Open the VS Code terminal inside the ResumeAI folder.

Windows
python -m venv .venv
.venv\Scripts\activate

Install everything:

pip install -r requirements.txt

Start the website:

uvicorn app:app --reload

You should see something similar to:

Uvicorn running on http://127.0.0.1:8000
